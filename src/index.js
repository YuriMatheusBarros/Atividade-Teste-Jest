const { iniciarJogo, reiniciarJogo } = require('./jogo');
const { fazerJogada } = require('./jogadas');

iniciarJogo();

// Exemplo de jogadas
// fazerJogada(0, 0);
// fazerJogada(1, 1);
// reiniciarJogo();
